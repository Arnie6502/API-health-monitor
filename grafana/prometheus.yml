# Prometheus scrape config for the API Health Monitor.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "api-health-monitor"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]
        labels:
          service: "api-health-monitor"
